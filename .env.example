# Algolia DocSearch is optional in local and CI builds.
# When the search values are present, the navbar search UI is enabled.
# When the agent values are present, the /ask-ai assistant page is enabled.

DOCSEARCH_APP_ID=
DOCSEARCH_API_KEY=
DOCSEARCH_INDEX_NAME=
DOCSEARCH_AGENT_ID=
DOCSEARCH_AGENT_URL=
